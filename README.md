
# ZestyPing

Changes from minimal:
- Removed sparklines panel.
- **Interval** and **Timeout** are now dropdowns with presets and a **Custom…** option.
  - Interval presets (seconds): 0.1, 0.2, 0.5, 1.0, 2.0
  - Timeout presets (ms): 100, 200, 300, 500, 1000, 1500, 2000

## Setup
```powershell
pip install -r requirements.txt
python .\app.py
```
